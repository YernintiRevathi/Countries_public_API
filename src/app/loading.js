// This is a special file recognized by Next.js. Because it's named loading.js, 
// Next.js will automatically show this component as a placeholder while the data in page.js is being fetched.
export default function Loading(){
    // A simple heading to inform the user that the content is on its way.
    
    return <h2 className="text-2xl font-bold text-center p-12">Loading Countries ...</h2>
}